define(
	[
		'jquery',
		'core/ajax',
	], function(
		$,
		Ajax
	){


	var getCourse = function(courseid){
		var request = {
            methodname: 'local_deleteoldcourses_get_course_by_id',
            args: {
				courseid:courseid
			}
        };

        var promise = Ajax.call([request])[0];

        promise.then(function(request) {
			console.log(request);
		}).catch;


		/*var requests = Ajax.call([
				methodname: 'local_deleteoldcourses_get_course_by_id',
				args: {
					courseid:courseid
				}
			]
		);
		requests[0].then(function(request) {
			console.log(request);
		}).catch;*/
	}


	var init = function(){
		var contextid = getCourse("11416");
	}

	return {
		'init':init,
	}
});